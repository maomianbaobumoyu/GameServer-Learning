上次学习的是基于C的小根堆定时器，这次来学习C++11的小根堆定时器，更加简洁和全面。

```c++
//超时回调函数
typedef std::function<void()> TimeoutCallBack;
//时钟类型(高精度)
typedef std::chrono::high_resolution_clock Clock;
//毫秒型
typedef Clock::time_point TimeStamp;
```

